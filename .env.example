# Descope
NEXT_PUBLIC_DESCOPE_PROJECT_ID=P39y2AtmSiEzB6oP1cjgx2GQUE1Y
NEXT_PUBLIC_DESCOPE_FLOW_ID=sign-up-or-in

# Prisma uses these two vars directly
POSTGRES_PRISMA_URL=postgresql://USER:PASSWORD@POOLER_HOST/DB?connect_timeout=15&sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://USER:PASSWORD@DIRECT_HOST/DB?sslmode=require

# Optional compatibility aliases if you also use DATABASE_URL naming
DATABASE_URL=${POSTGRES_PRISMA_URL}
DATABASE_URL_UNPOOLED=${POSTGRES_URL_NON_POOLING}

# Optional Neon/Vercel metadata vars
POSTGRES_URL=postgresql://USER:PASSWORD@POOLER_HOST/DB?sslmode=require
POSTGRES_USER=USER
POSTGRES_HOST=POOLER_HOST
POSTGRES_PASSWORD=PASSWORD
POSTGRES_DATABASE=DB
POSTGRES_URL_NO_SSL=postgresql://USER:PASSWORD@POOLER_HOST/DB

# Optional future LiteLLM integration
LITELLM_ADMIN_URL=http://localhost:4000
LITELLM_MASTER_KEY=replace-me
